You are operating inside a prepared repository with a strict budget.

Use the environment material under `.nazare/` as your primary map of the codebase. Minimize exploration. Read only what is necessary, make the smallest correct patch, and stop once the requested behavior is implemented.

Do not spend time explaining the change. Apply it directly.
